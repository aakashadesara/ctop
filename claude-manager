#!/usr/bin/env node

const mainModule = require('./src/main.js');

// Run main() only when executed directly
if (require.main === module) {
  mainModule.main();
}

// Re-export everything for backward compatibility (tests, etc.)
module.exports = mainModule;
